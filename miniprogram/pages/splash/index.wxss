.splash-page {
  width: 100vw;
  height: 100vh;
  overflow: hidden;
  background: #2D1E12; /* 兜底深褐色，防止图片加载瞬间白屏 */
}

/* 核心修正：使用 fixed 确保背景图铺满全屏 */
.full-bg {
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  z-index: 0;
  will-change: transform; /* 提升 PNG 动画性能 */
  animation: slowZoom 12s ease-in-out infinite; 
}

.splash-mask {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.4); /* 遮罩暗度，可根据 PNG 色彩微调 */
  z-index: 1;
}

.splash-content {
  position: relative;
  z-index: 2;
  width: 100vw;
  height: 100vh;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding-top: 40rpx; /* 微调垂直中心，匹配设计稿比例 */
  box-sizing: border-box;
}

/* UI 图片尺寸：已根据设计稿拉大占比 */
.main-design {
  width: 96%; /* 宽度从 85% 增加到 96% 以增强张力 */
  height: 85vh;
  animation: fadeInUp 1.2s ease-out;
}

.loading-text {
  position: absolute;
  bottom: 80rpx;
  font-size: 22rpx;
  color: rgba(255, 255, 255, 0.35);
  letter-spacing: 2rpx;
}

/* 动画定义 */
@keyframes fadeInUp {
  0% { opacity: 0; transform: translateY(40rpx); }
  100% { opacity: 1; transform: translateY(0); }
}

@keyframes slowZoom {
  0%, 100% { transform: scale(1.0); }
  50% { transform: scale(1.1); }
}